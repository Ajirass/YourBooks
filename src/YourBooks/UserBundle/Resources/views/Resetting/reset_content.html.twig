<div class="become_reader">
    <h1>Réinitialisation du mot de passe</h1>
    <form action="{{ path('your_books_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">
        {{ form_widget(form) }}
        <div class="submit resetting">
            <input type="submit" value="{{ 'resetting.reset.submit'|trans({}, 'FOSUserBundle') }}" />
        </div>
    </form>
</div>
