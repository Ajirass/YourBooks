{% extends 'YourBooksMainBundle::base.html.twig' %}
{% block body %}

    <div class="engagements">

        <h1>Mentions légales</h1>

        <div class="content">
            {% for mentionLegale in mentionsLegales %}
                <h2>{{ mentionLegale.title }}</h2>
                <p>{{ mentionLegale.content }}</p>
            {% endfor %}
        </div>
    </div>
{% endblock body %}